<%= form_for(@shopitem_tag) do |f| %>
  <% if @shopitem_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shopitem_tag.errors.count, "error") %> prohibited this shopitem_tag from being saved:</h2>

      <ul>
      <% @shopitem_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :shopitem_id %><br>
    <%= f.number_field :shopitem_id %>
  </div>
  <div class="field">
    <%= f.label :tag %><br>
    <%= f.text_field :tag %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
