<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "eShop" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
    <script src='https://www.google.com/recaptcha/api.js'></script>
  </head>
  <% if !flash[:notice].nil? %>
    <% case flash[:notice].to_s.split(';')[0] %>
    <% when "addtocart" then %>
      <body onLoad="$('#prompt_addtocart').foundation('reveal', 'open');">
    <% else %>
      <body>
    <% end %>
  <% else %>
    <body>
  <% end %>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <%= render 'layouts/warning' %>
    <main role="main">
      <%= yield %>
    </main>
    <footer>
      <%= render 'layouts/footer'%>
    </footer>
    <%= javascript_include_tag "application" %>
  </body>
</html>