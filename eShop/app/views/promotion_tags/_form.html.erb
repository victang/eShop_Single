<%= form_for(@promotion_tag) do |f| %>
  <% if @promotion_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@promotion_tag.errors.count, "error") %> prohibited this promotion_tag from being saved:</h2>

      <ul>
      <% @promotion_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :promotion_id %><br>
    <%= f.number_field :promotion_id %>
  </div>
  <div class="field">
    <%= f.label :tag %><br>
    <%= f.text_field :tag %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
